import 'package:flutter/material.dart';

class AccessMenu extends StatelessWidget{
  const AccessMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Access"),);
  }
}