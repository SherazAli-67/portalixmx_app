import 'package:flutter/material.dart';

class PaymentsMenu extends StatelessWidget{
  const PaymentsMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Payments menu"),);
  }
}